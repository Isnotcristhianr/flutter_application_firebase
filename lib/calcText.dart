import 'package:flutter/material.dart';

class calcText extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Aqui debe ir la calculadora"));
  }
}
